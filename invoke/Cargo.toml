[package]
edition = "2021"
name = "invoke"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
invoke_derive = {path = "../invoke_derive"}
memx = "0.1"
uuid = {version = "1.1", features = ["v4"]}
